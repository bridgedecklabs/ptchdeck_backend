# Allowed frontend origin for CORS
# Local dev: http://localhost:5173
# Production: https://ptchdeck.com
FRONTEND_URL=http://localhost:5173

# ── SMTP (add when you need email sending from the backend) ──
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=hello@ptchdeck.com
# SMTP_PASSWORD=your_16char_app_password
# CONTACT_TO_EMAIL=hello@ptchdeck.com
